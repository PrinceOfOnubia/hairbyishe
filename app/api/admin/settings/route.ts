import { cookies } from "next/headers";import { NextResponse } from "next/server";import { prisma } from "../../../lib/prisma";import { ADMIN_COOKIE,verifyAdminSession } from "../../../admin/auth";
export async function POST(request:Request){const store=await cookies();if(!verifyAdminSession(store.get(ADMIN_COOKIE)?.value))return NextResponse.json({error:"Unauthorized"},{status:401});const body=await request.json();const data={businessName:body.businessName||"HairByIshe",locations:body.locations||["Akure","Lagos"],instagram:body.instagram,tiktok:body.tiktok,phone:body.phone,whatsapp:String(body.whatsapp).replace(/\D/g,""),deliveryText:body.deliveryText||"Nationwide Delivery Across Nigeria",bankName:body.bankName||"Wema Bank",bankAccountName:body.bankAccountName||"HairByIshe Enterprises",bankAccountNumber:body.bankAccountNumber||"1234567890",footerText:body.footerText||"Thoughtfully sourced hair. Beautifully made units. Made personal, just for you.",defaultProductDescription:body.defaultProductDescription||"Premium quality hair."};const settings=await prisma.storeSettings.upsert({where:{id:"store"},update:data,create:{id:"store",...data}});return NextResponse.json({ok:true,settings})}
