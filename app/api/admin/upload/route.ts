import { put } from "@vercel/blob";
import { cookies } from "next/headers";
import { NextResponse } from "next/server";
import { ADMIN_COOKIE,verifyAdminSession } from "../../../admin/auth";
export async function POST(request:Request){const store=await cookies();if(!verifyAdminSession(store.get(ADMIN_COOKIE)?.value))return NextResponse.json({error:"Unauthorized"},{status:401});const data=await request.formData();const file=data.get("file");const allowed=["image/jpeg","image/png","image/webp","video/mp4","video/quicktime"];if(!(file instanceof File)||!allowed.includes(file.type))return NextResponse.json({error:"Choose a JPG, PNG, WEBP, MP4 or MOV file"},{status:400});if(file.size>50*1024*1024)return NextResponse.json({error:"Media must be under 50 MB"},{status:400});const blob=await put(`products/${Date.now()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,"-")}`,file,{access:"public",addRandomSuffix:true});return NextResponse.json({url:blob.url,pathname:blob.pathname,type:file.type.startsWith("video/")?"video":"image"})}
